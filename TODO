- Correos "enviar a"
- Email.php: añadir un "format=flowed" al Content-Type
- Reflexionar acerca de cambios de fecha de expiración
- Dar una solución al problema de las páginas 404, en el código está
  predeterminado que la aplicación está en el raíz, y no se permite usar
  funciones como site_url().
- Migración a 1.3:

   * Convertir a InnoDB las tablas
   * [desde svn]: alter table antivirus add constraint fk_ant foreign key(fid) references ficheros(fid) on delete cascade;
